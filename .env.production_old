# GPS WATCH - CONFIGURAZIONE PRODUZIONE VPS
# Questo file viene usato quando NODE_ENV=production

# Database Configuration (PostgreSQL locale sulla VPS)
DB_HOST=localhost
DB_NAME=gpswatch
DB_USER=gpsuser
DB_PASSWORD=GpsWatch2025
DB_PORT=5432

# VPS TCP Server (server Node.js per comandi agli orologi)
VPS_TCP_SERVER=http://localhost:3000

# Base URL per chiamate API interne
NEXT_PUBLIC_BASE_URL=http://localhost:3001

# JWT Secret per autenticazione
JWT_SECRET=gps-watch-super-secret-key-2025

# Watch Configuration
WATCH_PASSWORD=123456

# Logging
ENABLE_FILE_LOGGING=true
LOG_LEVEL=info
